# Famebook Cloud - Render Environment Configuration

# === Server ===
PORT=4000
NODE_ENV=production

# === Database ===
# Render automatycznie utworzy zmienną, jeśli połączysz bazę MongoDB (MongoDB on Render)
# ale jeśli masz własny klaster Atlas, możesz wkleić URL np:
# MONGO_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/famebook
MONGO_URI=${MONGO_URI}

# === JWT ===
JWT_SECRET=${JWT_SECRET}

# === Frontend CORS origin ===
# Adres frontendu na Renderze
CORS_ORIGIN=https://famebook-cloud-frontend.onrender.com

# === Optional debug flags ===
LOG_LEVEL=info
ENABLE_CORS=true
